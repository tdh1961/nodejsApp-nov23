pipeline {
    agent any
    
    stages {
        stage ("Dep install"){
            agent {docker {image 'node:14'
            args '-u root:sudo'}}
            steps {
                //sh 'sudo shown 0755 npm'
                sh 'npm install'
            }
        }
    }
}